(If you send any requests with the Accept header set to "application/json", it will also return the JSON data for that endpoint)

Get an user's profile information as JSON
```
GET /users/jeffalo/__data.json
```
